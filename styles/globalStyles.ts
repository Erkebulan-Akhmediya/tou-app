import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
    container: {
        borderWidth: 1,
        borderRadius: 20,
        padding: 10
    },
})

export default globalStyles;